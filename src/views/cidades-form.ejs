<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Nova cidade — Operações CIM</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    :root{--bg:#0b1220;--panel:#0f172a;--panel2:#111827;--border:#1f2937;--muted:#9ca3af;--text:#e5e7eb;--blue:#2563eb;--shadow:0 10px 30px rgba(0,0,0,.35);--r:14px}
    *{box-sizing:border-box}
    html,body{margin:0;background:var(--bg);color:var(--text);font-family:system-ui,-apple-system,Segoe UI,Roboto}
    a{color:inherit;text-decoration:none}
    .top{position:sticky;top:0;z-index:10;background:linear-gradient(to bottom,rgba(17,24,39,.86),rgba(17,24,39,.70));border-bottom:1px solid var(--border);backdrop-filter:saturate(140%) blur(8px)}
    .top-in{max-width:1160px;margin:0 auto;padding:14px 20px;display:flex;align-items:center;justify-content:space-between}
    .brand{display:flex;gap:10px;align-items:center}
    .badge{display:grid;place-items:center;width:30px;height:30px;border-radius:10px;background:linear-gradient(135deg,var(--blue),#0ea5e9);color:#fff;font-weight:900;box-shadow:0 6px 16px rgba(37,99,235,.35)}
    .container{max-width:820px;margin:0 auto;padding:28px 20px 56px}
    .card{background:linear-gradient(180deg,var(--panel),var(--panel2));border:1px solid var(--border);border-radius:var(--r);box-shadow:var(--shadow);padding:20px}
    .muted{color:var(--muted)}
    .btn{display:inline-flex;align-items:center;gap:8px;padding:10px 14px;border-radius:10px;border:1px solid var(--border);background:#111826;color:var(--text)}
    .btn:hover{filter:brightness(1.08)} .btn-blue{background:var(--blue);border-color:rgba(37,99,235,.6);color:#fff}
    .form{display:grid;gap:14px}
    label{font-size:13px;color:var(--muted)}
    input[type="text"],input[type="tel"]{width:100%;padding:11px 12px;border-radius:10px;border:1px solid var(--border);background:#0b1325;color:var(--text);outline:none}
    input[type="file"]{color:var(--muted)}
    .row{display:flex;gap:10px;flex-wrap:wrap}
    .err{background:#3b0d0d;border:1px solid #7f1d1d;color:#fecaca;border-radius:10px;padding:10px 12px}
  </style>
</head>
<body>
  <div class="top"><div class="top-in">
    <div class="brand"><span class="badge">C</span><div>Operações CIM <div class="muted">Nova cidade</div></div></div>
    <div class="row">
      <a class="btn" href="/cidades">Voltar</a>
    </div>
  </div></div>

  <div class="container">
    <div class="card">
      <h2 style="margin:0 0 12px">Cadastrar cidade</h2>

      <% if (errors && errors.length) { %>
        <div class="err" style="margin-bottom:12px">
          <ul style="margin:0 0 0 18px">
            <% errors.forEach(function(e){ %><li><%= e %></li><% }) %>
          </ul>
        </div>
      <% } %>

      <form class="form" method="post" action="/cidades/nova" enctype="multipart/form-data">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

        <div>
          <label>Nome da cidade</label>
          <input type="text" name="nome" value="<%= (values && values.nome) ? values.nome : '' %>" required>
        </div>

        <div>
          <label>Corporação/Secretaria (opcional)</label>
          <input type="text" name="corporacao" value="<%= (values && values.corporacao) ? values.corporacao : '' %>">
        </div>

        <div>
          <label>Comandante/Responsável (opcional)</label>
          <input type="text" name="comandante" value="<%= (values && values.comandante) ? values.comandante : '' %>">
        </div>

        <div>
          <label>Contato (opcional)</label>
          <input type="text" name="contato" value="<%= (values && values.contato) ? values.contato : '' %>">
        </div>

        <div>
          <label>Logo (opcional, PNG/JPG/WebP, até 2MB)</label>
          <input type="file" name="logo" accept="image/*">
        </div>

        <div class="row" style="justify-content:flex-end">
          <a class="btn" href="/cidades">Cancelar</a>
          <button class="btn btn-blue" type="submit">Salvar</button>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
