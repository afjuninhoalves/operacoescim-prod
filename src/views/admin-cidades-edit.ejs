<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8" />
  <title>Editar Cidade</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body { background:#0f172a; color:#e5e7eb; font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; }
    .wrap { max-width:900px; margin:24px auto; padding:0 16px; }
    .card { background:#111827; border-radius:12px; box-shadow:0 8px 22px rgba(0,0,0,.35); padding:20px; margin-bottom:20px; }
    .grid { display:grid; grid-template-columns: 1fr 1fr; gap:14px; }
    label { display:block; margin-bottom:6px; color:#cbd5e1; font-size:14px; }
    input[type="text"], input[type="tel"], input[type="file"]{
      width:100%; padding:10px 12px; border-radius:8px; border:1px solid #334155; background:#0b1220; color:#e5e7eb;
    }
    .btn { display:inline-block; padding:8px 12px; border-radius:8px; background:#2563eb; color:#fff; text-decoration:none; font-weight:600; }
    .btn:hover { background:#1d4ed8; }
    .err { background:#7f1d1d; color:#fecaca; padding:10px 12px; border-radius:8px; margin-bottom:12px; }
    img.logo { width:64px; height:64px; object-fit:contain; background:#0b1220; border-radius:8px; }
  </style>
</head>
<body>
  <div class="wrap">
    <div class="card">
      <h2 style="margin-top:0;">Editar Cidade #<%= cidade.id %></h2>

      <% if (errors && errors.length) { %>
        <div class="err">
          <ul style="margin:0; padding-left:18px;">
            <% errors.forEach(e => { %><li><%= e %></li><% }) %>
          </ul>
        </div>
      <% } %>

      <form method="post" action="/admin/cidades/<%= cidade.id %>/editar" enctype="multipart/form-data">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <div class="grid">
          <div>
            <label>Nome da Cidade</label>
            <input type="text" name="nome" value="<%= cidade.nome %>" required>
          </div>

          <div>
            <label>Logo (opcional)</label>
            <input type="file" name="logo" accept="image/png,image/jpeg,image/webp">
            <% if (cidade.logo_path) { %>
              <div style="margin-top:8px;"><img class="logo" src="<%= cidade.logo_path %>" alt="logo atual"></div>
            <% } %>
          </div>

          <div>
            <label>Corporação</label>
            <input type="text" name="corporacao" value="<%= cidade.corporacao %>" required>
          </div>

          <div>
            <label>Comandante</label>
            <input type="text" name="comandante" value="<%= cidade.comandante %>" required>
          </div>

          <div>
            <label>Telefone Contato</label>
            <input type="tel" name="telefone_contato" value="<%= cidade.telefone_contato %>" required>
          </div>
        </div>

        <div style="margin-top:16px; display:flex; gap:10px;">
          <button class="btn" type="submit">Salvar</button>
          <a class="btn" href="/admin/cidades">Voltar</a>
        </div>
      </form>
    </div>
  </div>
</body>
</html>
