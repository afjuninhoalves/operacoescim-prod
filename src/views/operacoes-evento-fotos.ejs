<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8" />
  <title>Fotos do evento — <%= operacao?.nome || 'Operação' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <style>
    body{background:#0f172a;color:#e5e7eb;font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;margin:0}
    .wrap{max-width:1100px;margin:24px auto;padding:0 16px}
    .card{background:#111827;border:1px solid #1f2937;border-radius:12px;box-shadow:0 8px 22px rgba(0,0,0,.35);padding:16px}
    .row{display:flex;gap:10px;align-items:center;flex-wrap:wrap}
    .btn{display:inline-block;padding:8px 12px;border-radius:8px;background:#2563eb;color:#fff;text-decoration:none;font-weight:600}
    .btn:hover{background:#1d4ed8}
    .btn-danger{background:#ef4444}
    h2,h3{margin:8px 0}
    .grid{display:grid;gap:14px;grid-template-columns:repeat(auto-fill,minmax(160px,1fr))}
    .thumb{border:1px solid #1f2937;border-radius:10px;overflow:hidden;background:#0b1220}
    .thumb img{display:block;width:100%;height:140px;object-fit:cover}
    .thumb .meta{padding:8px;font-size:12px;color:#9ca3af;display:flex;justify-content:space-between;align-items:center}
    label{display:block;margin-top:10px;color:#cbd5e1}
    input[type=file]{margin-top:6px}
    form.inline{display:inline}
  </style>
</head>
<body>
  <div class="wrap">
    <div class="row" style="justify-content:space-between">
      <div>
        <h2>Fotos do evento</h2>
        <div><strong>Operação:</strong> <%= operacao?.nome %></div>
        <div><strong>Evento:</strong> <%= evento?.tipo %> — <%= detalhe || '' %></div>
      </div>
      <div class="row">
        <a class="btn" href="/operacoes/<%= operacao.id %>">Voltar</a>
        <a class="btn" href="/operacoes/<%= operacao.id %>/fotos">Galeria da operação</a>
      </div>
    </div>

    <div class="card" style="margin-top:12px;">
      <h3>Anexar novas fotos</h3>
      <form method="post" action="/operacoes/<%= operacao.id %>/eventos/<%= evento.id %>/fotos" enctype="multipart/form-data">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <label>Selecione 1 ou mais arquivos (até 10)</label>
        <input type="file" name="fotos" multiple accept="image/*">
        <div style="margin-top:10px">
          <button class="btn" type="submit">Enviar</button>
        </div>
      </form>
    </div>

    <div class="card" style="margin-top:12px;">
      <h3>Fotos anexadas (<%= fotos.length %>)</h3>
      <% if (!fotos.length) { %>
        <div style="color:#9ca3af">Nenhuma foto neste evento ainda.</div>
      <% } else { %>
        <div class="grid">
          <% fotos.forEach(f => { %>
            <div class="thumb">
              <a href="<%= f.path %>" target="_blank" rel="noopener">
                <img src="<%= f.path %>" alt="foto">
              </a>
              <div class="meta">
                <span><%= new Date(f.created_at||Date.now()).toLocaleString('pt-BR') %></span>
                <form class="inline" method="post" action="/operacoes/<%= operacao.id %>/eventos/<%= evento.id %>/fotos/<%= f.id %>/delete">
                  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button class="btn btn-danger" type="submit" onclick="return confirm('Remover esta foto?')">Remover</button>
                </form>
              </div>
            </div>
          <% }) %>
        </div>
      <% } %>
    </div>
  </div>
</body>
</html>
